# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Environment / secrets
.env
.env.local
backend/.env
serviceAccount*.json

# Node
node_modules/
.next/
out/
npm-debug.log*

# Probe output
backend/duffel_hold_probe_result.json

# The design system is VENDORED at frontend/design-system/ and stays TRACKED:
# a judge cloning this repo must be able to build without a directory that
# only exists on one machine.

# Raw handoff bundle (superseded by the vendored copy) and paper drafts
windfall-design-component-review/
Windfall Design Comp Review/
*.pdf
